enum ProjectApprovalStatus {
  pending
  reviewing
  approved
  rejected
}

enum ProjectOperationStatus {
  ongoing
  paused
  cancelled
  completed
  notStarted
}

enum SubmissionStatus {
  draft
  submitted
  withdrawn
}

enum UserRole {
  superAdmin
  generalManager
  financeManager
  partner
}
